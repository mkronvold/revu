{
  "name": "revu",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@10.9.2",
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "PROXY_NET_EXTERNAL=false PROXY_NET_NAME=revu-dev-proxy-net docker compose -f docker-compose.yml -f docker-compose.dev.yml up web api db",
    "dev:api": "npm run dev --workspace @revu/api",
    "dev:web": "npm run dev --workspace @revu/web",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "validate": "npm test && npm run typecheck && npm run build",
    "compose:config": "docker compose config -q",
    "compose:config:dev": "PROXY_NET_EXTERNAL=false PROXY_NET_NAME=revu-dev-proxy-net docker compose -f docker-compose.yml -f docker-compose.dev.yml config -q",
    "deploy:pull": "docker compose pull",
    "deploy:up": "docker compose up -d",
    "db:up": "docker compose up -d db",
    "db:migrate": "bash ./scripts/db-migrate.sh",
    "db:down": "docker compose down"
  }
}
